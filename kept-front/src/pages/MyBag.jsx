function MyBag({ onOpenMenu }) {
  return (
    <main>
      <header>
        <h1>My Bag (GUI 완료 후 작업 예정)</h1>

        <button type="button" onClick={onOpenMenu}>
          메뉴
        </button>
      </header>
    </main>
  );
}

export default MyBag;
